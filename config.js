// ---------------------------------------------------------------------------
// Zipo Fare Logger — Supabase configuration
// ---------------------------------------------------------------------------
// Paste your project's URL and *anon* (public) key below. Both are safe to ship
// in client-side code: the anon key only grants what your Row Level Security
// (RLS) policies allow, and this app's RLS requires an authenticated session
// for every read and write. Do NOT paste the service_role key here — that key
// bypasses RLS and must never touch the browser.
//
// Find these under: Supabase dashboard → Project Settings → API
//   • Project URL      → SUPABASE_URL
//   • Project API keys → anon / public → SUPABASE_ANON_KEY
// ---------------------------------------------------------------------------

window.ZIPO_CONFIG = {
  SUPABASE_URL: "https://xgbxwrscbqyreejtoemn.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_9n3ixnC1bVAp6sFDKWY90Q_W2bZwqPB"
};
